Algoritmo "funcoes_matematicas"

Var

   x, y, z, a, b, c, d : real

Inicio

   x <- 2
   y <- 3
   z <- -7
   a <- EXP(x, y)
   b <- RAIZQ(y)
   c <- PI
   d <- ABS(z)

   escreval(a)
   escreval(b)
   escreval(c)
   escreval(d)

Fimalgoritmo